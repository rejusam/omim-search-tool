# Test fixtures

`search_response.json` is a captured OMIM `entry/search` response with
`include=geneMap`, used by the offline test suite.

Source: [SYNTHETIC — built from OMIM documentation, NOT yet reconciled against the
live API].

If synthetic: field names and especially the form of `phenotypeInheritance`
are unverified. Reconcile against a live response before relying on the
tool's output.
